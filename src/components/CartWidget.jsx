import React from "react";

const CartWidget = () => {
  return (
    <div>
      <button className="botonCarrito">
        <img src="/img/shopping-cart.svg" alt="carrito" className="Carrito" />2
      </button>
    </div>
  );
};

export default CartWidget;
